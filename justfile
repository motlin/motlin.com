# `just --list --unsorted`
default:
    @just --list --unsorted

# `npm start -- --port 4000`
start:
    npm start -- --port 4000

# `npm run build`
build:
    npm run build

# `npm run serve`
serve:
    npm run serve

# `npm run deploy`
deploy:
    npm run deploy